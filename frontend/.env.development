# Loaded ONLY by `npm run dev` (Vite development mode). `npm run build` ignores
# this file, so production keeps the https://api.parohia-santana-1.ro fallback
# from src/lib/api.js.
VITE_BACKEND_URL=http://localhost:8000
